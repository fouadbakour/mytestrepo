general:
  # if the application is *not* in the root of the repo but
  # in the sub-directory called "ios-app"
  # build_dir: ios-app

machine:
  xcode:
    version: "7.2"
  environment:
    # please specify your code signing identity name here
    GYM_CODE_SIGNING_IDENTITY: "iPhone Distribution: Fouad Bakour (LPSJ2534Z4)"

deployment:
  staging:
    branch: master
    commands:
      - gym --workspace "project.xcworkspace" --scheme "testappNewScheme" --clean
      - deliver --ipa "testapp.ipa" --submit_for_review
